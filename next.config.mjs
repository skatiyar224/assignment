/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    trailingSlash: true, // Optional: Adds trailing slashes to URLs
    basePath: '/assignment', // Replace with your repository name if it's a project page
    assetPrefix: '/assignment/', // Also replace if applicable
  };
  
  export default nextConfig;
  